The P(r) inversion package contains the computational components
necessary to perform a P(r) inversion on data. It contains C
files that do most of the computational work. The python module
that performs this inversion uses NumPy and SciPy as well as thes
C extensions.